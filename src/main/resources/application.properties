spring.application.name=sss-server
server.servlet.context-path=/sss
#spring.datasource.url=jdbc:postgresql://localhost:5432/postgres
#spring.datasource.username=postgres
#spring.datasource.password=SSS1994
#spring.datasource.driver-class-name=org.postgresql.Driver

#spring.jpa.hibernate.ddl-auto=update
#spring.jpa.show-sql=true
#spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

spring.datasource.url=${DB_URL:jdbc:postgresql://sss-db-4.cjucwww0s4yc.ap-south-1.rds.amazonaws.com:5432/sssdb}
spring.datasource.username=${DB_USERNAME}
spring.datasource.password=${DB_PASSWORD}

spring.datasource.driver-class-name=org.postgresql.Driver

spring.jpa.hibernate.ddl-auto=validate
# Disabled: formatting + printing every executed statement adds real overhead
# on every request. Flip back to true only when actively debugging a query.
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

spring.flyway.enabled=true
spring.flyway.locations=classpath:db/migration
spring.flyway.baseline-on-migrate=true
# Bumped from 0: the schema was bootstrapped directly by Hibernate (see
# README-recovery.md) rather than by replaying V1-V42, so on first contact
# with this database Flyway should record itself as already caught up
# through the highest existing migration (42) instead of trying to execute
# any of their SQL bodies against tables that already exist.
spring.flyway.baseline-version=42
spring.flyway.baseline-description=hibernate-bootstrap

app.frontend-url=${FRONTEND_URL:http://localhost:3000}
# Local disk storage/serving kept only as a read-only fallback for images
# uploaded before the Cloudinary migration — new uploads no longer write here.
app.upload-dir=${UPLOAD_DIR:uploads}
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

# Cloudinary image storage — no defaults, same as DB/mail credentials below;
# the deployment environment must supply these.
cloudinary.cloud-name=${CLOUDINARY_CLOUD_NAME}
cloudinary.api-key=${CLOUDINARY_API_KEY}
cloudinary.api-secret=${CLOUDINARY_API_SECRET}

spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=${MAIL_USERNAME}
spring.mail.password=${MAIL_PASSWORD}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

spring.redis.host=localhost
spring.redis.port=6379
spring.session.store-type=redis
spring.session.redis.namespace=spring:session
spring.session.timeout=1800 # 30 minutes
